#!/bin/bash
# This default command keeps this container alive, waiting for interaction

echo "--- Ready to accept interactive commands"
echo "--- Connect to me with 'docker compose [--profile ...] exec -it bashlib bash'"
sleep infinity
